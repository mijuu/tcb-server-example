'use strict';

module.exports = {
  env_id: 'your env',
  private_key_id: 'your private_key_id',
  private_key: 'your private_key',
  secretId: 'your secretId', // 云环境下运行非必要，本地开发需要
  secretKey: 'your secretKey' // 云环境下运行非必要，本地开发需要
}
